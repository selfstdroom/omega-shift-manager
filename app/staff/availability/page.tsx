import { mockAvailabilities, mockProjects } from '@/data/mockData';
export default function Page(){return <div><h1 className="mb-4 text-2xl font-bold">勤務可能日登録</h1><div className="space-y-3">{mockProjects.map(p=>{const av=mockAvailabilities.find(a=>a.project_id===p.id&&a.staff_id==='staff-1');return <div className="rounded bg-white p-4 shadow" key={p.id}><b>{p.title}</b><p className="text-sm text-slate-600">{p.work_date} {p.start_time}-{p.end_time} / {p.location}</p><div className="mt-3 grid gap-2 sm:grid-cols-[180px_1fr]"><select className="rounded border p-2" defaultValue={av?.status ?? 'unavailable'}><option value="available">available</option><option value="conditional">conditional</option><option value="unavailable">unavailable</option></select><input className="rounded border p-2" defaultValue={av?.note} placeholder="メモ（条件、移動予定など）" /></div></div>})}</div></div>}
