import Link from 'next/link';
import { mockProjects, mockWorkplaces } from '@/data/mockData';
export default function Page(){return <div><div className="mb-4 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><h1 className="text-2xl font-bold">案件一覧</h1><Link className="rounded bg-slate-900 px-3 py-2 text-center text-white" href="/admin/projects/new">作成</Link></div><div className="space-y-3">{mockProjects.map(p=><div className="rounded bg-white p-4 shadow" key={p.id}><div className="flex flex-col gap-1 sm:flex-row sm:items-center sm:justify-between"><b>{p.title}</b><span className="text-sm text-slate-500">{mockWorkplaces.find(w=>w.id===p.workplace_id)?.name}</span></div><p className="text-sm text-slate-600">{p.work_date} {p.start_time}-{p.end_time} / {p.location}</p><p className="text-sm font-medium">必要{p.required_people}名・リーダー{p.required_leaders}名</p>{p.note&&<p className="mt-1 text-sm text-slate-500">備考: {p.note}</p>}</div>)}</div></div>}
