'use client';
import { useEffect, useMemo, useState } from 'react';
import { Badge } from '@/components/ui/Badge';import { Card } from '@/components/ui/Card';import { Input } from '@/components/ui/Input';import { PageHeader } from '@/components/ui/PageHeader';import { Select } from '@/components/ui/Select';
import { getProjectFill, getDemoAssignments } from '@/lib/demo';import { mockProjects, mockWorkplaces } from '@/lib/mockData';import { getSupabaseBrowserClient } from '@/lib/supabaseClient';import type { Assignment, Project, Workplace } from '@/lib/types';
export default function Page(){const [items,setItems]=useState<Project[]>(mockProjects);const [workplaces,setWorkplaces]=useState<Workplace[]>(mockWorkplaces);const [assignments,setAssignments]=useState<Assignment[]>(getDemoAssignments());const [q,setQ]=useState('');const [wp,setWp]=useState('all');useEffect(()=>{(async()=>{const s=getSupabaseBrowserClient(); if(!s)return; const [{data:ps},{data:ws},{data:as}]=await Promise.all([s.from('projects').select('*').order('work_date'),s.from('workplaces').select('*'),s.from('assignments').select('*')]); if(ps?.length)setItems(ps as Project[]); if(ws?.length)setWorkplaces(ws as Workplace[]); if(as?.length)setAssignments(as as Assignment[]);})()},[]);const filtered=useMemo(()=>items.filter(p=>(wp==='all'||p.workplace_id===wp)&&`${p.title} ${p.location}`.toLowerCase().includes(q.toLowerCase())),[items,q,wp]);return <div><PageHeader title="案件管理" description="デモでは編集より閲覧性を優先し、充足状況が一目で分かるカードUIにしています。" /><div className="mb-5 grid gap-3 sm:grid-cols-2"><Input placeholder="案件名・場所で検索" value={q} onChange={e=>setQ(e.target.value)}/><Select value={wp} onChange={e=>setWp(e.target.value)}><option value="all">全事業所</option>{workplaces.map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</Select></div><div className="grid gap-4">{filtered.map(p=>{const f=getProjectFill(p,assignments);return <Card key={p.id} className="p-5 transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-bold text-slate-950">{p.title}</h2><Badge tone={f.peopleOk?'green':'orange'}>人数 {f.assigned}/{p.required_people}</Badge><Badge tone={f.leadersOk?'blue':'red'}>リーダー {f.leaders}/{f.requiredLeaders}</Badge></div><p className="mt-2 text-sm text-slate-500">{workplaces.find(w=>w.id===p.workplace_id)?.name ?? '事業所'} · {p.location}</p></div><div className="grid grid-cols-2 gap-3 text-sm sm:grid-cols-4 lg:min-w-[520px]"><Info label="日付" value={p.work_date}/><Info label="時間" value={`${p.start_time}-${p.end_time}`}/><Info label="必要人数" value={`${p.required_people}名`}/><Info label="必要L" value={`${f.requiredLeaders}名`}/></div></div>{p.note&&<p className="mt-4 rounded-xl bg-slate-50 p-3 text-sm text-slate-600">{p.note}</p>}</Card>})}</div></div>}
function Info({label,value}:{label:string;value:string}){return <div className="rounded-xl bg-slate-50 p-3"><p className="text-xs font-semibold text-slate-400">{label}</p><p className="mt-1 font-bold text-slate-800">{value}</p></div>}
