'use client';
import { useState } from 'react';
import { Badge } from '@/components/ui/Badge';import { Button } from '@/components/ui/Button';import { Card } from '@/components/ui/Card';import { EmptyState } from '@/components/ui/EmptyState';import { PageHeader } from '@/components/ui/PageHeader';import { StatCard } from '@/components/ui/StatCard';
import { getDemoAssignmentResults } from '@/lib/demo';import { mockAvailabilities, mockProfiles } from '@/lib/mockData';import type { AssignmentResult } from '@/lib/types';
export default function AutoAssignPage(){const [results,setResults]=useState<AssignmentResult[]>([]);const run=()=>setResults(getDemoAssignmentResults());const name=(id:string)=>mockProfiles.find(p=>p.id===id)?.name??id;const av=(pid:string,sid:string)=>mockAvailabilities.find(a=>a.project_id===pid&&a.staff_id===sid)?.status??'-';const ok=results.filter(r=>!r.warnings.length).length;const people=results.filter(r=>r.warnings.includes('人数不足')).length;const leaders=results.filter(r=>r.warnings.includes('リーダー不足')).length;return <div><PageHeader title="自動配置" description="最重要フローをデモデータで即実行できます。成功・不足・配置スタッフを確認してください。" actions={<Button onClick={run} className="min-h-14 min-w-64 rounded-2xl bg-gradient-to-r from-blue-600 to-slate-900 text-base shadow-lg shadow-blue-200">自動配置を実行</Button>} />{results.length>0&&<div className="mb-6 grid gap-4 sm:grid-cols-4"><StatCard label="対象案件" value={results.length}/><StatCard label="成功案件" value={ok} tone="green"/><StatCard label="人数不足" value={people} tone="orange"/><StatCard label="リーダー不足" value={leaders} tone="red"/></div>}{!results.length?<EmptyState title="配置結果はまだありません" description="ボタンを押すと、デモスタッフの勤務可否から自動配置を実行します。" action={<Button onClick={run}>今すぐ実行</Button>}/>:<div className="grid gap-4">{results.map(r=>{const st=!r.warnings.length?'配置成功':r.warnings.join('・');const tone=!r.warnings.length?'green':r.warnings.includes('人数不足')?'orange':'red';return <Card className={`border-l-4 p-5 ${!r.warnings.length?'border-l-green-500 bg-green-50/20':tone==='orange'?'border-l-orange-500':'border-l-red-500'}`} key={r.project.id}><div className="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between"><div><h2 className="text-lg font-bold">{r.project.title}</h2><p className="text-sm text-slate-600">{r.project.work_date} {r.project.start_time}-{r.project.end_time} · 必要{r.project.required_people}名 / L{Math.max(1,r.project.required_leaders??0)}名</p></div><Badge tone={tone}>{st}</Badge></div><div className="mt-4 grid gap-2 md:grid-cols-2">{r.assignments.map(a=><div className="flex items-center justify-between rounded-xl bg-white p-3 shadow-sm ring-1 ring-slate-100" key={a.staff_id}><span className="font-medium">{name(a.staff_id)}</span><span className="flex items-center gap-2 text-sm text-slate-600">希望:{av(r.project.id,a.staff_id)} {a.is_leader&&<Badge tone="blue">Leader</Badge>}</span></div>)}</div></Card>})}</div>}</div>}
