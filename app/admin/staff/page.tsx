'use client';
import { useEffect, useMemo, useState } from 'react';
import { Badge } from '@/components/ui/Badge';import { Button } from '@/components/ui/Button';import { Card } from '@/components/ui/Card';import { Input } from '@/components/ui/Input';import { PageHeader } from '@/components/ui/PageHeader';import { ResponsiveEditor } from '@/components/ui/ResponsiveEditor';import { Select } from '@/components/ui/Select';
import { getDemoShiftRows } from '@/lib/demo';import { mockProfiles, mockWorkplaces } from '@/lib/mockData';import { getSupabaseBrowserClient } from '@/lib/supabaseClient';
import type { Profile, StaffRole, Workplace } from '@/lib/types';
export default function Page(){const [items,setItems]=useState<Profile[]>(mockProfiles.filter(p=>p.role==='staff'));const [workplaces,setWorkplaces]=useState<Workplace[]>(mockWorkplaces);const [msg,setMsg]=useState('デモデータで操作できます');const [q,setQ]=useState('');const [wp,setWp]=useState('all');const [editing,setEditing]=useState<Profile|null>(null);async function load(){const s=getSupabaseBrowserClient(); if(!s)return; const [{data,error},{data:ws}]=await Promise.all([s.from('profiles').select('*').eq('role','staff').order('created_at'),s.from('workplaces').select('*')]); if(error)setMsg(`${error.message}（デモデータを表示中）`); else {if(data?.length)setItems(data); if(ws?.length)setWorkplaces(ws); setMsg('')}}useEffect(()=>{load()},[]);async function save(){if(!editing)return;setItems(a=>a.map(x=>x.id===editing.id?editing:x));const s=getSupabaseBrowserClient(); if(!s){setMsg('Supabase未接続のため画面上のみ保存しました');setEditing(null);return}const {error}=await s.from('profiles').update({name:editing.name,phone:editing.phone,staff_role:editing.staff_role,workplace_id:editing.workplace_id}).eq('id',editing.id); setMsg(error?`${error.message}（画面上のみ更新しました）`:'保存しました');setEditing(null)}function remove(){if(!editing||!confirm('このスタッフを無効化/削除しますか？'))return;setItems(a=>a.filter(x=>x.id!==editing.id));setMsg('デモ上で無効化しました');setEditing(null)}const filtered=useMemo(()=>items.filter(p=>(wp==='all'||p.workplace_id===wp)&&`${p.name} ${p.phone}`.toLowerCase().includes(q.toLowerCase())),[items,q,wp]);return <div><PageHeader title="スタッフ一覧" description="カードを開いて一覧画面のままスタッフ情報・役職を編集できます。" />{msg&&<p className="mb-3 text-sm text-blue-700">{msg}</p>}<div className="mb-4 grid gap-3 sm:grid-cols-2"><Input placeholder="名前・電話で検索" value={q} onChange={e=>setQ(e.target.value)}/><Select value={wp} onChange={e=>setWp(e.target.value)}><option value="all">全事業所</option>{workplaces.map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</Select></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map(p=>{const shifts=getDemoShiftRows(p.id);return <Card key={p.id} role="button" tabIndex={0} onClick={()=>setEditing(p)} onKeyDown={e=>{if(e.key==='Enter')setEditing(p)}} className="group cursor-pointer p-5 transition hover:-translate-y-1 hover:ring-2 hover:ring-blue-200 focus:outline-none focus:ring-4 focus:ring-blue-200"><div className="flex items-start justify-between gap-3"><div><h2 className="text-lg font-bold text-slate-950">{p.name}</h2><p className="mt-1 text-sm text-slate-500">📍 {workplaces.find(w=>w.id===p.workplace_id)?.name??'未設定'}</p></div><Badge tone={p.staff_role==='leader'?'blue':'slate'}>{p.staff_role==='leader'?'👑 Leader':'Staff'}</Badge></div><div className="mt-4 grid grid-cols-2 gap-2 text-sm"><Info label="今月の配置数" value={`${shifts.length}件`}/><Info label="直近の勤務予定" value={shifts[0]?.projects?.work_date??'未定'}/></div><p className="mt-4 text-xs font-bold text-blue-600 opacity-0 transition group-hover:opacity-100">クリックして編集 →</p></Card>})}</div><ResponsiveEditor open={!!editing} title={editing?.name??''} subtitle="スタッフ詳細" onClose={()=>setEditing(null)}>{editing&&<div className="space-y-4"><Label text="名前"><Input value={editing.name} onChange={e=>setEditing({...editing,name:e.target.value})}/></Label><Label text="電話番号"><Input value={editing.phone??''} onChange={e=>setEditing({...editing,phone:e.target.value})}/></Label><Label text="所属事業所"><Select value={editing.workplace_id??''} onChange={e=>setEditing({...editing,workplace_id:e.target.value})}>{workplaces.map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</Select></Label><Label text="役職"><Select value={editing.staff_role} onChange={e=>setEditing({...editing,staff_role:e.target.value as StaffRole})}><option value="staff">staff</option><option value="leader">leader</option></Select></Label><div className="grid grid-cols-2 gap-3 pt-3"><Button onClick={save}>保存</Button><Button variant="danger" onClick={remove}>無効化/削除</Button></div></div>}</ResponsiveEditor></div>}
function Info({label,value}:{label:string;value:string}){return <div className="rounded-2xl bg-slate-50 p-3"><p className="text-xs font-bold text-slate-400">{label}</p><p className="mt-1 font-bold text-slate-800">{value}</p></div>}
function Label({text,children}:{text:string;children:React.ReactNode}){return <label className="block"><span className="mb-1 block text-sm font-bold text-slate-700">{text}</span>{children}</label>}
