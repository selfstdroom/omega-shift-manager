import Link from 'next/link';
import { StatusBadge } from '@/components/StatusBadge';
import { mockProfiles, mockWorkplaces } from '@/data/mockData';

export default function Page(){return <div><div className="mb-4 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><h1 className="text-2xl font-bold">スタッフ一覧・作成</h1><Link className="rounded bg-slate-900 px-3 py-2 text-center text-white" href="/admin/staff/new">登録</Link></div><div className="overflow-x-auto rounded bg-white shadow"><table className="w-full min-w-[640px]"><thead><tr className="bg-slate-100 text-left"><th className="p-3">氏名</th><th>役職</th><th>事業所</th><th>電話番号</th></tr></thead><tbody>{mockProfiles.filter(p=>p.role==='staff').map(p=><tr className="border-b" key={p.id}><td className="p-3 font-medium">{p.name}</td><td><StatusBadge tone={p.staff_role==='leader'?'blue':'slate'}>{p.staff_role}</StatusBadge></td><td className="text-sm text-slate-600">{mockWorkplaces.find(w=>w.id===p.workplace_id)?.name}</td><td className="text-sm text-slate-600">{p.phone}</td></tr>)}</tbody></table></div></div>}
